# 智慧路灯管理系统 - 前端接口需求分析文档

## 文档说明

本文档汇总了智慧路灯管理系统前端项目中所有已定义但后端尚未实现的接口需求。接口按照业务模块分类，包含接口路径、请求方式、功能描述、参数说明和返回值结构。

---

## 一、已实现接口清单

以下接口后端已实现，前端可正常调用：

### 1.1 路灯设备模块 (light.js)

| 接口路径 | 方法 | 功能描述 |
|---------|------|---------|
| /lights/page | GET | 分页查询路灯列表 |
| /lights | GET | 获取所有路灯 |
| /lights/districts | GET | 获取行政区列表 |
| /lights/roads | GET | 获取路段列表 |
| /lights/device-types | GET | 获取设备类型列表 |
| /lights/{id} | GET | 获取路灯详情 |
| /lights | POST | 新增路灯 |
| /lights | PUT | 更新路灯 |
| /lights/{id} | DELETE | 删除路灯 |
| /lights/batch-switch | POST | 批量开关灯 |
| /lights/{id}/brightness | PUT | 设置路灯亮度 |
| /lights/stats | GET | 路灯状态统计 |

### 1.2 传感器数据模块 (sensor.js)

| 接口路径 | 方法 | 功能描述 |
|---------|------|---------|
| /sensor-data/page | GET | 分页查询传感器数据 |
| /sensor-data/latest/{lightId} | GET | 获取路灯最新传感器数据 |
| /sensor-data/average/{lightId} | GET | 获取平均传感器数据 |
| /sensor-data | POST | 新增传感器数据 |

### 1.3 报警管理模块 (alert.js)

| 接口路径 | 方法 | 功能描述 |
|---------|------|---------|
| /alerts/page | GET | 分页查询报警 |
| /alerts/{id} | GET | 获取报警详情 |
| /alerts/{id}/handle | PUT | 处理报警 |
| /alerts/unhandled-count | GET | 获取未处理报警数量 |
| ws:/ws/alert | WebSocket | 告警实时推送 |

### 1.4 用户管理模块 (user.js)

| 接口路径 | 方法 | 功能描述 |
|---------|------|---------|
| /users/login | POST | 用户登录 |
| /users | GET | 获取用户列表 |
| /users | POST | 新增用户 |
| /users | PUT | 更新用户 |
| /users/{id} | DELETE | 删除用户 |

### 1.5 照明控制模块 - 远程控制 (control.js)

| 接口路径 | 方法 | 功能描述 |
|---------|------|---------|
| /lights/batch-switch | POST | 批量开关灯（复用light.js） |
| /lights/{id}/brightness | PUT | 设置路灯亮度（复用light.js） |

---

## 二、未实现接口清单（后端缺失）

以下接口前端已定义调用，但后端尚未实现，需要后端开发支持。

### 2.1 路灯设备模块 - 扩展接口 (light.js)

#### 2.1.1 分组统计接口

- **接口路径**: `GET /lights/group-stats`
- **功能描述**: 按行政区/路段/设备类型分组统计路灯数量
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | groupBy | string | 是 | 分组维度：district(行政区)/road(路段)/deviceType(设备类型) |

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": [
      {
        "groupName": "东城区",
        "total": 100,
        "online": 95,
        "offline": 3,
        "fault": 2
      }
    ]
  }
  ```

- **前端调用位置**: [DeviceLedger.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/devices/DeviceLedger.vue)

---

### 2.2 碳减排分析模块 (carbon.js)

#### 2.2.1 核心指标汇总接口

- **接口路径**: `GET /carbon/summary`
- **功能描述**: 获取指定周期内的总节电量、总减排量、节能率
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | type | string | 是 | 统计类型：monthly(月度每日)/yearly(年度每月) |
  | period | string | 是 | 统计周期：月度为 "YYYY-MM"，年度为 "YYYY" |

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": {
      "totalSavedPower": 305.6,
      "totalReducedCO2": 304.6,
      "energySavingRate": 87.8
    }
  }
  ```

#### 2.2.2 能耗趋势接口

- **接口路径**: `GET /carbon/trend`
- **功能描述**: 获取趋势数据（月度每日/年度每月）
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | type | string | 是 | 统计类型：monthly(月度每日)/yearly(年度每月) |
  | period | string | 否 | 统计周期：月度为 "YYYY-MM"，年度为 "YYYY" |

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": [
      {
        "period": "2026-07-01",
        "savedEnergy": 15.2,
        "reducedCo2": 15.1
      }
    ]
  }
  ```

#### 2.2.3 路段节电量对比接口

- **接口路径**: `GET /carbon/road-compare`
- **功能描述**: 按路段对比节电量
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | type | string | 是 | 统计类型：monthly/yearly |
  | period | string | 是 | 统计周期 |

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": [
      {
        "road": "长安街",
        "savedEnergy": 85.5
      }
    ]
  }
  ```

#### 2.2.4 能耗基准配置查询接口

- **接口路径**: `GET /carbon/baseline`
- **功能描述**: 获取传统钠灯基准能耗配置
- **请求参数**: 无

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": {
      "basePower": 250,
      "dailyHours": 11,
      "emissionFactor": 0.581
    }
  }
  ```

#### 2.2.5 能耗基准配置更新接口

- **接口路径**: `PUT /carbon/baseline`
- **功能描述**: 更新传统钠灯基准能耗配置
- **请求体**:
  ```json
  {
    "basePower": 250,
    "dailyHours": 11,
    "emissionFactor": 0.581
  }
  ```

- **返回值结构**:
  ```json
  {
    "code": 200,
    "message": "保存成功"
  }
  ```

#### 2.2.6 Excel报表导出接口

- **接口路径**: `GET /carbon/export`
- **功能描述**: 导出碳减排分析报表Excel文件
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | type | string | 是 | 统计类型 |
  | period | string | 是 | 统计周期 |

- **返回值**: Blob 文件流（Excel）

- **前端调用位置**: [CarbonAnalysis.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/carbon/CarbonAnalysis.vue)

---

### 2.3 照明控制模块 - 定时策略 (control.js)

#### 2.3.1 定时策略分页查询

- **接口路径**: `GET /control/strategies/page`
- **功能描述**: 分页查询定时策略列表
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | pageNum | number | 是 | 页码 |
  | pageSize | number | 是 | 每页条数 |

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": {
      "records": [],
      "total": 0,
      "pageNum": 1,
      "pageSize": 10
    }
  }
  ```

#### 2.3.2 定时策略列表

- **接口路径**: `GET /control/strategies`
- **功能描述**: 获取所有定时策略
- **请求参数**: 无
- **返回值**: 策略数组

#### 2.3.3 新增定时策略

- **接口路径**: `POST /control/strategies`
- **功能描述**: 新增定时策略
- **请求体**:
  ```json
  {
    "strategyName": "策略名称",
    "lightIds": [1, 2, 3],
    "onTime": "18:00",
    "offTime": "06:00",
    "brightness": 80,
    "enabled": true,
    "repeatDays": [1, 2, 3, 4, 5]
  }
  ```

#### 2.3.4 更新定时策略

- **接口路径**: `PUT /control/strategies`
- **功能描述**: 更新定时策略
- **请求体**: 同上，含 id 字段

#### 2.3.5 删除定时策略

- **接口路径**: `DELETE /control/strategies/{id}`
- **功能描述**: 删除指定定时策略

#### 2.3.6 启停定时策略

- **接口路径**: `PUT /control/strategies/{id}/enable`
- **功能描述**: 启用/停用定时策略
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | enabled | boolean | 是 | 是否启用 |

- **前端调用位置**: [TimedStrategy.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/control/TimedStrategy.vue)

---

### 2.4 照明控制模块 - 阈值联动控制 (control.js)

#### 2.4.1 获取阈值配置

- **接口路径**: `GET /control/threshold`
- **功能描述**: 获取阈值联动控制配置
- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": {
      "enabled": true,
      "lightOnThreshold": 30,
      "lightOffThreshold": 100,
      "lowBrightness": 100,
      "midBrightness": 60,
      "highBrightness": 30,
      "detectionPeriod": 60
    }
  }
  ```

#### 2.4.2 更新阈值配置

- **接口路径**: `PUT /control/threshold`
- **功能描述**: 更新阈值联动配置
- **请求体**: 同上结构

#### 2.4.3 一键启停阈值联动

- **接口路径**: `PUT /control/threshold/toggle`
- **功能描述**: 一键启用/停用阈值联动总开关
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | enabled | boolean | 是 | 是否启用 |

- **前端调用位置**: [ThresholdControl.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/control/ThresholdControl.vue)

---

### 2.5 操作日志模块 (operation-log.js)

#### 2.5.1 操作日志分页查询

- **接口路径**: `GET /operation-logs/page`
- **功能描述**: 分页查询操作日志
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | pageNum | number | 是 | 页码 |
  | pageSize | number | 是 | 每页条数 |
  | username | string | 否 | 操作人筛选 |
  | operationType | string | 否 | 操作类型筛选 |
  | startTime | string | 否 | 开始时间 |
  | endTime | string | 否 | 结束时间 |

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": {
      "records": [
        {
          "id": 1,
          "username": "admin",
          "operationType": "light_control",
          "description": "开启路灯",
          "result": "成功",
          "createTime": "2026-07-05 10:00:00"
        }
      ],
      "total": 100,
      "pageNum": 1,
      "pageSize": 10
    }
  }
  ```

#### 2.5.2 记录操作日志

- **接口路径**: `POST /operation-logs`
- **功能描述**: 前端控制操作后调用，记录操作日志
- **请求体**:
  ```json
  {
    "operationType": "light_control",
    "description": "开启路灯",
    "result": "成功"
  }
  ```

- **前端调用位置**: 
  - [OperationLog.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/control/OperationLog.vue)
  - [OperationAudit.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/system/OperationAudit.vue)
  - log.js 工具函数（全局操作日志记录）

---

### 2.6 系统配置模块 (config.js)

#### 2.6.1 获取系统参数配置

- **接口路径**: `GET /system/config`
- **功能描述**: 获取全局系统参数配置

#### 2.6.2 更新系统参数配置

- **接口路径**: `PUT /system/config`
- **功能描述**: 更新全局系统参数配置

#### 2.6.3 获取告警规则列表

- **接口路径**: `GET /system/alert-rules`
- **功能描述**: 获取告警规则配置列表

#### 2.6.4 新增告警规则

- **接口路径**: `POST /system/alert-rules`
- **功能描述**: 新增告警规则

#### 2.6.5 更新告警规则

- **接口路径**: `PUT /system/alert-rules`
- **功能描述**: 更新告警规则

#### 2.6.6 删除告警规则

- **接口路径**: `DELETE /system/alert-rules/{id}`
- **功能描述**: 删除指定告警规则

- **前端调用位置**: [SystemConfig.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/system/SystemConfig.vue)

---

### 2.7 AI 智能运维助手模块 (assistant.js)

#### 2.7.1 发送对话消息

- **接口路径**: `POST /ai/assistant/chat`
- **功能描述**: 发送对话消息，支持多轮对话
- **请求体**:
  ```json
  {
    "sessionId": "sess_xxx",
    "message": "用户问题"
  }
  ```

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": {
      "reply": "AI回复内容"
    }
  }
  ```

#### 2.7.2 获取历史会话

- **接口路径**: `GET /ai/assistant/history`
- **功能描述**: 获取指定会话的历史消息
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | sessionId | string | 是 | 会话ID |

#### 2.7.3 知识库分页查询

- **接口路径**: `GET /ai/assistant/knowledge`
- **功能描述**: 分页查询知识库FAQ条目
- **请求参数**: pageNum, pageSize, keyword

#### 2.7.4 新增知识库条目

- **接口路径**: `POST /ai/assistant/knowledge`
- **功能描述**: 新增知识库FAQ条目

#### 2.7.5 更新知识库条目

- **接口路径**: `PUT /ai/assistant/knowledge`
- **功能描述**: 更新知识库FAQ条目

#### 2.7.6 删除知识库条目

- **接口路径**: `DELETE /ai/assistant/knowledge/{id}`
- **功能描述**: 删除知识库条目

- **实现建议**: 先本地知识库关键词匹配，匹配不到则调用大模型 API

- **前端调用位置**:
  - [AiAssistant.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/ai/AiAssistant.vue)
  - [AiKnowledge.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/ai/AiKnowledge.vue)
  - [AiFloatBall.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/components/AiFloatBall.vue)

---

### 2.8 AI 自适应预测调光模块 (predict.js)

#### 2.8.1 获取预测结果

- **接口路径**: `GET /ai/predict/result`
- **功能描述**: 获取未来24小时逐时推荐亮度
- **请求参数**:
  | 参数名 | 类型 | 必填 | 说明 |
  |--------|------|------|------|
  | lightId | number | 是 | 路灯ID |

- **返回值结构**:
  ```json
  {
    "code": 200,
    "data": [
      {
        "hour": "20:00",
        "predictedBrightness": 85,
        "confidence": 0.92
      }
    ]
  }
  ```

#### 2.8.2 启用预测模式

- **接口路径**: `POST /ai/predict/apply`
- **功能描述**: 一键启用预测模式，自动下发调光指令
- **请求体**:
  ```json
  {
    "lightId": 1
  }
  ```

#### 2.8.3 停用预测模式

- **接口路径**: `POST /ai/predict/stop`
- **功能描述**: 停用预测模式
- **请求体**:
  ```json
  {
    "lightId": 1
  }
  ```

#### 2.8.4 效果对比

- **接口路径**: `GET /ai/predict/compare`
- **功能描述**: 预测模式 vs 固定阈值模式能耗差异 + 预测准确率
- **请求参数**: lightId, startDate, endDate

- **前端调用位置**: [AiPredict.vue](file:///c:/Users/29674/Desktop/Smart-Street-Light-System-main/Smart-Street-Light-System/frontend/src/views/ai/AiPredict.vue)

---

## 三、接口优先级建议

### P0 - 核心业务功能（必须优先实现）

1. 定时策略 CRUD 接口 - 照明控制核心功能
2. 阈值联动控制接口 - 自动调光核心功能
3. 操作日志接口 - 系统审计必要功能
4. 碳减排核心指标接口 - 核心数据展示

### P1 - 重要功能（第二优先级）

1. 碳减排趋势和路段对比接口 - 数据可视化
2. 系统参数配置接口 - 系统管理
3. 告警规则配置接口 - 告警管理
4. AI 助手对话接口 - AI 功能基础
5. 路灯分组统计接口 - 台账统计

### P2 - 增强功能（第三优先级）

1. AI 预测调光接口 - 高级 AI 功能
2. 知识库管理接口 - AI 助手增强
3. 碳减排 Excel 导出接口 - 报表功能

---

## 四、前端容错处理说明

对于所有后端缺失的接口，前端已做如下容错处理：

1. **错误静默**: 接口调用失败时不阻塞主流程，使用空数据或默认值兜底
2. **本地缓存**: AI 助手对话历史使用 localStorage 本地持久化
3. **状态提示**: 图表区域显示"后端接口缺失"的空状态提示
4. **本地导出**: 碳减排报表导出接口失败时，使用 xlsx 库本地生成兜底

后端接口实现后，前端无需修改即可自动对接。
